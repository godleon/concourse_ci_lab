platform: linux

image_resource:
  type: docker-image
  source: 
    repository: registry.access.redhat.com/rhel7.3

params: { 'ANSIBLE_HOST_KEY_CHECKING': 'False' }


inputs:
- name: osp_test_resource

# output:
# - name: osp_test_resource

run:
  path: sh
  args:
    - -exc
    - |
      env
      find .
      echo "Hello Slack! ${date}" | tee osp_test_resource/07.slack_notification/slack_message.txt
       