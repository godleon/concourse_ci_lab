platform: linux

image_resource:
  type: docker-image
  source:
    # repository: ubuntu
    # tag: xenial
    repository: rallyforge/rally
    tag: latest

inputs:
- name: git-osp
- name: git-semver
- name: git-results

outputs:
- name: rally_output

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    env
  
    bash git-osp/09.rally/scripts/run_keystone_benchmark.sh